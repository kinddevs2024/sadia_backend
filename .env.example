# Database
# Use Blob Storage in production (Vercel Blob)
# BLOB_READ_WRITE_TOKEN=your_vercel_blob_token

# Authentication
JWT_SECRET=your-secret-key-change-in-production

# Terminal Payment Provider
# Options: mock | (future: real terminal providers)
TERMINAL_PROVIDER=mock

# Mock Terminal Behavior
# Options: success | fail | timeout
TERMINAL_MOCK_MODE=success

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3000/api
